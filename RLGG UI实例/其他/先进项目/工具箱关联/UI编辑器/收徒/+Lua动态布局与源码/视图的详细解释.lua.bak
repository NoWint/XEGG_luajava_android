在日常我们玩ui的人中经常谈到视图，但是
视图「也就是View」很容易搞混。安卓开发里的视图有，
子视图（Child View）、根视图（Root View）
和父视图（Parent View）
他们是视图（View）层级结构中的基本概念，
它们共同构成了应用程序的用户界面。
以下是对这些术语的详细解释：
子视图（Child View）

定义：子视图是指在视图层级结构中，被另一个视图所包含的视图。简单来说，如果一个视图A被另一个视图B所包围或包含，那么A就是B的子视图。
特点：子视图的大小和位置通常受其父视图的约束和影响。在Android的布局系统中，子视图会按照父视图指定的布局参数（如宽度、高度、边距等）进行排列和展示。
示例：在一个LinearLayout中，添加了一个Button作为其子视图。此时，Button就是LinearLayout的子视图，其位置和大小将受到LinearLayout布局参数的影响。

根视图（Root View）

定义：根视图是指在视图层级结构中的最顶层视图，它是整个布局结构的起点和基础。在Android中，一个Activity的根视图通常是指其DecorView，它是Activity窗口的根视图，包含了整个应用界面的内容。
特点：根视图是视图层级结构中的最高级别视图，它通常不包含在任何其他视图内部。根视图的大小和位置将直接决定整个应用界面的大小和布局。
获取方式：在Android中，可以通过调用Activity的getWindow().getDecorView().getRootView()方法来获取根视图。但需要注意的是，getRootView()方法在某些情况下可能并不直接返回DecorView，而是返回DecorView的子视图，这取决于具体的Android版本和框架实现。

父视图（Parent View）

定义：父视图是指在视图层级结构中，包含其他视图的视图。如果一个视图A包含了另一个视图B，那么A就是B的父视图。
特点：父视图负责管理和布局其子视图。在Android的布局系统中，父视图会根据其布局参数和子视图的布局要求，来安排子视图的位置和大小。
获取方式：在Android中，可以通过调用视图（View）的getParent()方法来获取其父视图。但需要注意的是，getParent()方法返回的是一个ViewParent对象，它可能是一个ViewGroup（即视图组，是视图的容器），也可能是一个View（在特殊情况下，如一个视图直接作为另一个视图的父视图，尽管这种情况不常见）。如果确定父视图是ViewGroup，则可以通过类型转换来获取具体的ViewGroup对象。

综上所述，子视图、根视图和父视图是Android视图层级结构中的基本概念，它们共同构成了应用程序的用户界面。理解和掌握这些概念对于进行Android界面开发和布局优化至关重要。参考：1.Android视图绘制流程完全解析,带你一步步深入了解View(二)2.Android 视图(View)概述3.android getRootView4.Android开发中获取父视图的方法及应用